use config::{Config, AudioBookShelfConfig};

pub struct ABSClient {
    pub token: String, 
    pub server: String,
}

impl ABSClient {
    pub fn login(abs_config: &AudioBookShelfConfig){
        
    }
}